import { Client } from 'discord.js'

// 定期的な地震通知システム（Wolfix APIに移行したため無効化）
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function startEqAutoNotify(_client: Client) {
    console.log('🔄 地震自動通知システム: Wolfix APIに移行したため、このシステムは無効化されています')
    console.log('   緊急地震速報は Wolfix API監視システム で提供されます')
    // JMA API監視は無効化（Wolfix APIを使用）
}